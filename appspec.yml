version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/fanmix
permissions:
  - object: /home/ubuntu/fanmix/
    owner: root
    group: root
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
